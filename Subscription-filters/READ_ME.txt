Using apply_subscription_filter_to_all_log_groups.py

Install Requirements:
Ensure Python and boto3 are installed. Use pip install boto3 if needed.
Set AWS Credentials:

Export your AWS keys:
export AWS_ACCESS_KEY_ID='your_access_key'
export AWS_SECRET_ACCESS_KEY='your_secret_key'

Configure Script:
Replace 'account_id' with your AWS account number.

Run the Script:
Execute in the terminal: python3 apply_subscription_filter_to_all_log_groups.py